import 'package:flutter/material.dart';

class HomePageMobileView extends StatelessWidget {
  const HomePageMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Mobile page'),);
  }
}
